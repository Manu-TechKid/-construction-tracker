import React from 'react';
import { Box, Typography } from '@mui/material';

const Profile = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        My Profile
      </Typography>
      <Typography>Profile settings will be shown here.</Typography>
    </Box>
  );
};

export default Profile;
