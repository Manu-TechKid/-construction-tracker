services:
  - type: web
    name: construction-tracker
    env: node
    plan: starter
    buildCommand: npm install && cd client && npm install --legacy-peer-deps && npm run build && cd ../server && npm install && npm run build
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
